/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.freepik.com'], // 👈 Add this line
  },
  // ... keep any other existing configs you have
}

module.exports = nextConfig